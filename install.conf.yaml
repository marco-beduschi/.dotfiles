- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/code
    - ~/work

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
