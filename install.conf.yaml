- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    #    ~/.vim: vim
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/code
    - ~/work

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
#  - [git submodule update --init --recursive, Installing submodules]
