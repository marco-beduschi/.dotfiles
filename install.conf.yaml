- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    - ~/code
    - ~/work

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
